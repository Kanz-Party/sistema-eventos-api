name: Deploy to FTP

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16' 

    - name: Install Yarn
      run: npm install -g yarn

    - name: Install dependencies
      run: yarn install


    - name: Upload to FTP
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: ftp.kanzparty.com.br
        username: kanzparty
        password: rznDpiB7!J
        local-dir: ./ 
        server-dir: ../apps_nodejs/tickets/
